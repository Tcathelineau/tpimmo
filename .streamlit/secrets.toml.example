# Copiez ce fichier en `secrets.toml` (ignoré par git) pour tester en local,
# ou collez son contenu dans Streamlit Community Cloud : App settings > Secrets.
#
# Si `app_password` n'est pas défini, le dashboard reste accessible sans mot de passe
# (comportement par défaut en local).

app_password = "changez-moi"
